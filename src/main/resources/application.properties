# ===============================
# = APPLICATION NAME
# ===============================
spring.application.name=celebrato-backend

# ===============================
# = DATABASE CONFIGURATION
# ===============================
# Use environment variables for Render; fallback to local defaults
spring.datasource.url=jdbc:mysql://<db-host>:3306/celebrato_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mugi4715
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# = JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================
# = SERVER PORT (Render sets PORT env variable)
# ===============================
server.port=${PORT:8080}
